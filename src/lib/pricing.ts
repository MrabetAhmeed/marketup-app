// TODO: Implement in Phase 7 (Boost & Sponsoring)
// - computeVat(priceHT: number, vatRate: number): { vatAmount: number; priceTTC: number }
// - formatMoneyHT(value: number): string  → "50,00 DT HT"
// - formatMoneyTTC(priceHT: number, vatRate: number): string → "59,50 DT TTC"
// - formatMoney(value: number): string → "1 250 DT" (non-breaking space)
