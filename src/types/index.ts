export type CompanyStatus = "pending" | "active" | "rejected" | "suspended" | "deleted";

export type ProfileStatus = "incomplete" | "pending" | "active" | "rejected" | "disabled";

export type ProfileKind = "brandup" | "traceup" | "linkup";

export type UserRole = "OWNER" | "SUPER_ADMIN";

export type SupportedLang = "fr" | "ar" | "en";

export type CompanyType = "B2B" | "B2C";

export type TransactionType = "boost" | "sponsoring";

export type TransactionStatus = "pending" | "paid" | "refunded" | "failed";

export type RseReceiptStatus = "pending" | "validated" | "rejected";
